apiVersion: v1
kind: Service
metadata:
  name: decisionos
  namespace: decisionos
  labels:
    app.kubernetes.io/name: decisionos
    app.kubernetes.io/component: server
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: decisionos
    app.kubernetes.io/component: server
